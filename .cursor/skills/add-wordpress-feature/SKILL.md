---
name: add-wordpress-feature
description: Add a WordPress feature that uses the official PHP and JS SDKs
---

# Add a WordPress feature

Use the PHP SDK for server work and enqueue the JS/TS SDK for UI. Update the
user-facing README for installers (activate, settings), not git workflow.
