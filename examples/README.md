# WordPress plugin examples

Standalone WordPress site that installs this plugin. Native PHP + WP-CLI +
SQLite (no Docker).

| Example | Port |
| --- | --- |
| [wordpress-site](wordpress-site/) | 8080 |
